import React from 'react'
import shortid from 'shortid'
import { Player } from 'video-react'
import { withPrefix } from 'gatsby-link'


const ClockbeatsMapSection = ({ title, body, services }) => (
  <section className="full-wrapper text-center default-section" style={{ backgroundImage: "url('https://placehold.it/1920x1080')" }}>
    <img src="" alt="" className="img-fluid" />

  </section>

)

export default ClockbeatsMapSection
